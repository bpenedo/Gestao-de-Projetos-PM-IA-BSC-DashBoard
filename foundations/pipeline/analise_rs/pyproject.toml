[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "analise_rs"
version = "0.1.0"
description = "Classificador Lean acelerado em Rust (PyO3) para o Framework PM IA."
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
