-- CPM + índice de criticidade: em quantos % das simulações cada tarefa caiu no caminho
-- crítico (mais honesto que o CPM determinístico). Posições p50 para o Gantt.
SELECT * FROM cronograma_critico;
